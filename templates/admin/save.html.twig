{% extends 'base.html.twig' %}

{% block page_stylesheets %}

{% endblock %}

{% block body %}
    <div class="panel box box-warning">
        <div class="box-header with-border">
            <h4 class="box-title">
                Sauvegarde de la base de données
            </h4>
        </div>

        <div class="box-body">
            <a href=" {{ path('admin_save') }}" class="btn btn-success">
                <i class="fa fa-edit"></i> Sauvegarder
            </a>
        </div>

        <div class="panel box box-warning">
            <div class="box-header with-border">
                <h4 class="box-title">
                    Restauration de la base de données
                </h4>
            </div>
            <div class="box-body">
                {{ form_start(form) }}
                    <div class="form-group">
                        <label>Fichier de restauration de la base </label> (fichier SQL)
                        {{ form_widget(form.uploadedDatabaseFile, {'attr': {'class': 'form-control'}}) }}
                    </div>

                    <div class="box-footer">
                        <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modal-default">Restaurer</button>
                    </div>

                    <!-- NON AFFICHÉ - modal - boite de dialogue pour confirmer la suppression -->
                    <div class="modal fade" id="modal-default" style="display: none;">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">×</span></button>
                                    <h4 class="modal-title">Confirmation</h4>
                                </div>
                                <div class="modal-body">
                                    <p>Êtes-vous sûr de vouloir restaurer la base de donnée?</p>
                                    <p>Attention, cette manoeuvre est risquée,
                                        si votre fichier n'est pas à jour, vous risquez de perdre des données,
                                        voir toutes les données si votre fichier est corrompu.
                                        Il est important de ne pas exectuer cette restauration à la légère au rique de compromettre tout le logiciel.</p>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Non</button>
                                    <button type="submit" class="btn btn-danger"><i class="fa fa-trash"></i> Oui</button>
                                </div>
                            </div><!-- /.modal-content -->
                        </div><!-- /.modal-dialog -->
                    </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block page_javascripts %}
{% endblock %}
